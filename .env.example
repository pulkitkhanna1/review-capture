# Render + SendGrid (production)

SENDGRID_API_KEY=SG.xxxx
SENDGRID_FROM=Yukta <yukta@revops.shop>
PM_EMAIL=your-inbox@example.com
NODE_ENV=production

# Optional — AI review drafts (falls back to template if unset)
# ANTHROPIC_API_KEY=
